# работает ТОЛЬКО во время сборки образа (docker build) - чтобы лишнее не попало в образ!

# зависимости (в контейнере зависимости устанавливаются заново):
node_modules/

# сборки:
dist/

# env-файлы (env передаём через docker run / compose):
.env
.env.*

# git (тяжёлый и не нужен для runtime):
.git/
.gitignore

# логи:
logs/
npm-debug.log*
yarn-debug.log* 
yarn-error.log*
pnpm-debug.log*

# docker-логи:
docker/*.log # в папке docker/
*.docker.log # в корне проекта

# coverage:
coverage/
.nyc_output/

# кэши:
.eslintcache
.npm/
.tmp/
.cache/

# ide:
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# OS мусор:
.DS_Store # Mac OS
Thumbs.db # Windows

# документация / лицензия:
*.md
LICENSE
