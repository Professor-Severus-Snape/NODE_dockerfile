{
  "name": "library",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "author": "professor-severus-snape",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "docker:build:prod": "docker build -f docker/Dockerfile.prod -t professorseverussnape/library .",
    "docker:run:prod": "docker run -d --rm --name container-library -e PORT=3000 -p 3000:3000 professorseverussnape/library",
    "docker:push:prod": "docker push professorseverussnape/library",
    "docker:build:dev": "docker build -f docker/Dockerfile.dev -t image-library-dev .",
    "docker:run:dev": "docker run -it --rm --name container-library-dev -e PORT=3000 -p 3000:3000 -v $(pwd):/app -v /app/node_modules image-library-dev"
  },
  "dependencies": {
    "ejs": "^4.0.1",
    "express": "^5.2.1",
    "express-ejs-layouts": "^2.5.1",
    "multer": "^2.0.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.1",
    "globals": "^17.3.0",
    "nodemon": "^3.1.13"
  }
}
